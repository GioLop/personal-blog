<!DOCTYPE html>
<html lang="en">
<head>
  <%-  include('../partials/head') %>
  <title>Document</title>
</head>
<body>
  <h2><%= formTitle %></h2>
  <p>All field marked with * are mandatory.</p>
  <form action="<%= handler %>" method="<%= method %>">
    <div>
      <label for="articleTitle">Article Title *</label>
      <br>
      <input 
        type="text"
        id="articleTitle"
        name="articleTitle"
        value="<% if (locals?.article) { %><%= article.title.trim() %><% } else { %><% }%>">
    </div>

    <div>
      <label for="publishDate">Publishing Date *</label>
      <br>
      <input
        type="date"
        id="publishDate"
        name="publishDate"
        value="<% if (locals?.article) { %><%= new Date(article.publishDate).toISOString(article.publishDate).slice(0, 10) %><% } else { %><% } %>">
    </div>

    <div>
      <label for="content">Content *</label>
      <br>
      <textarea
        id="content"
        name="content"><% if (locals?.article) {%><%- article.body.trim()%><%} else {%><%}%></textarea>
    </div>

    <input type="submit" value="<%= submitText %>">
  </form>
</body>
</html>